# AGENTS.md

本文件约束在本项目中工作的所有 AI agent。

项目自带 `.qnote` sidecar 批注机制：源文件 `foo.ext` 的批注保存在同目录的 `foo.ext.qnote`（JSON 格式），批注不修改源文件本身。

---

## Agent：批注读写分离（annotation-separation）

### 读文档时

- 读取任何文档的同时，检查是否存在对应的 `.qnote` 批注文件（`<文件名>.qnote`）；存在则一并读取，把批注视为该文档上下文的一部分。
- 批注中如有用户要求、纠错记录、注意事项，按批注执行任务；已处理完的批注按项目约定标记 resolved，不得自动删除用户批注。

### 写文档时

正文与批注严格分离：

- **正文只写结论和结果**：当前最终的、正确的内容本身。
- **批注承载过程与提醒**：解释、原因、注意事项、待确认事项、历史问题和纠错说明，写入对应 `.qnote` 批注文件，不进正文。

**纠错规则**：某处内容被用户（或 agent）纠正后，重新输出时正文只写正确结果；"之前错在哪、为什么不能用 X"之类的纠正性说明一律放入批注，不得残留在正文。

**过程与理解同样入批注**：agent 本次修改了什么、为什么这样改、对需求与上下文的理解和解读，一律放入批注。正文的效果标准：读起来像一次性生成的最终结果，看不出任何中间修正的痕迹。

**反例（禁止）**：用户指出"做辣椒炒肉不应放红烧肉"之后，正文从此写成"辣椒炒肉（不要放红烧肉）"。

**正例**：正文写"辣椒炒肉"（做法按正确结果呈现）；批注中记录"2026-09-19 用户纠正：不放红烧肉"。

同理，正文中不得出现面向 agent 或维护者的元指令，例如"注意：这里曾经……"、"此文件曾被修改为……"——这类内容一律属于批注。

### 通过标准

1. agent 读文档时能发现并加载对应 `.qnote` 批注；
2. 被纠正的内容重新生成后，正文不含任何纠正性提示或历史说明，只含最终结果；
3. 所有过程性解释、注意事项均出现在批注文件中，而非正文；
4. agent 创建的每条批注都带 `[Agent: …]` 署名且 `source=agent`，与用户批注一眼可分。

---

## 规范：批注署名（agent 批注与用户批注必须区分）

`.qnote` schema 中有 `source: "user" | "agent"` 字段，agent 批注必须用足所有区分手段，不依赖单一字段：

1. **强制署名**：agent 创建的每条批注，`text` 必须以签名标记开头，格式为 `[Agent: <agent名或模型名>, <YYYY-MM-DD>]`，例如 `[Agent: claude-code, 2026-09-19] 这里需要确认……`。
2. **source 字段**：同一批注的 `source` 必须设为 `"agent"`。
3. **来源语义**：
   - `source=user` 的批注是用户指令或结论，agent 必须执行，只能标记 resolved，不得修改其内容、不得删除；
   - `source=agent` 的批注是 agent 的工作记录与解释，后续 agent 可以核对、更新、resolve；确认已过时的 agent 批注可以说明理由后删除，但不得顺手清理用户批注；
   - 来源字段缺失或可疑时，一律按用户批注对待（保守处理）。
4. **不得伪造**：agent 不得创建未署名的批注、不得把用户批注改成 agent 批注或反之、不得冒用用户口吻书写批注。

本规范为默认行为，无需用户在任务中另行指示；后续 agent 读文档、写批注时自动执行。
